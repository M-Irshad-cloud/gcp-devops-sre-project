apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: orders-api-podmonitoring
  labels:
    app: orders-api
spec:
  selector:
    matchLabels:
      app: orders-api
  endpoints:
    - port: 8080
      interval: 30s
      path: /metrics
